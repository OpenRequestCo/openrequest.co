# openrequest.co
Marketing website for OpenRequest

Static HTML and CSS. Geist is self-hosted in `fonts/`. Tailwind is compiled into `styles.css` — the page does not load Google Fonts or a CDN.

Rebuild CSS after class changes:

```
npm install
npm run css
```

