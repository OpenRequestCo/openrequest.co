---
/**
 * Story section in Linear’s rhythm: title + copy, then one visual.
 */
interface Props {
  id?: string;
  eyebrow?: string;
  class?: string;
  /** Content measure before the visual. */
  measure?: 'narrow' | 'readable' | 'wide';
}

const { id, eyebrow, class: className, measure = 'readable' } = Astro.props;

const measureClass = {
  narrow: 'max-w-[32rem]',
  readable: 'max-w-[40rem]',
  wide: 'max-w-[52rem]',
}[measure];
---

<section
  id={id}
  class:list={['page-shell py-[var(--spacing-chapter)]', className]}
>
  {
    eyebrow && (
      <p class="type-caption mb-4">{eyebrow}</p>
    )
  }
  <div class={measureClass}>
    <slot />
  </div>
  {
    Astro.slots.has('visual') && (
      <div class="product-stage mt-10 lg:mt-12">
        <slot name="visual" />
      </div>
    )
  }
</section>
