---
interface Props {
  href?: string;
  variant?: 'primary' | 'secondary' | 'zinc' | 'text';
  class?: string;
}

const { href, variant = 'primary', class: className } = Astro.props;

const classes = [
  'inline-flex items-center justify-center gap-1.5 text-sm font-medium transition-colors duration-150',
  variant === 'primary' &&
    'rounded-3xl bg-accent px-3.5 py-3 text-white hover:bg-accent-hover',
  variant === 'zinc' &&
    'rounded-3xl bg-zinc-900 px-3.5 py-3 text-white hover:bg-zinc-800',
  variant === 'secondary' &&
    'rounded-3xl border border-border-strong bg-surface px-3.5 py-2 text-ink hover:bg-surface-muted',
  variant === 'text' && 'text-ink-muted hover:text-ink',
  className,
];

const Tag = href ? 'a' : 'button';
---

<Tag href={href} type={href ? undefined : 'button'} class:list={classes}>
  <slot />
</Tag>
